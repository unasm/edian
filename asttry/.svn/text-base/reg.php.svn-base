<?php
	/**
	 * 这个文件处理的是注册的函数
	 **/
	class Login extends Common
	{
		protected $userName,$user_id,$userPasswd;
		//一些常用的数据，保存到本地，方便调用	
		function __construct()
		{
;
		}
		public function index()
		{
			//对下面各种函数的调度，流程的处理，本身不处理逻辑
			$this->userInfoGet();
			$this->infoCheck();
			$this->imgCheck();
			$this->checkReady()
		}
		public function imgCode()
		{
			//生成图片验证码的地方

		}
		private function imgCheck()
		{
			//检查图片验证码
		}
		private function randNum()
		{
			//生成随机数字，提供给其他的函数调用,目前为imgCode服务
		}
		private function infoCheck($name,$passwd,$imgcode)
		{
			//对用户名称，密码，图片验证码进行安装，规范的验证	
		}
		private  function userInfoGet()
		{
			//从html页面获得用户的注册信息，保存到用户的本地,然后交由其他函数处理
		}
		private function checkReady()
		{
			//各种处理，验证检查完之后，进行数据库保存，用户信息初始化等信息，
		}

	}
?>
