<?php
/**
 * 这个文件进行的是不管是那些模块，都需要的关于用户的一些操作函数1
 **/
class Common extends Ci_Controller
{
	
	function __construct()
	{
		session_start();//大概session是必须开启的吧，这里首先开启，以后应该不需要了再写了
	}
	public function userDataInit($userName,$userId,$userPasswd,$userLoginTime)
	{
		//初始化用户信息的函数，就是设置用户的名字，id，密码，登录时间的函数，不论是什么用户，登录了都是需要这个操作的
	}
	public function reLoginTime()
	{
		//更新用户session保存时间的函数，每当用户有所操作的时候，调用这个函数，刷新永恒与的session
		
	}
}
?>
